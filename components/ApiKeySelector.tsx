import React from 'react';

/**
 * @deprecated This component is no longer in use. API key management is handled
 * exclusively via environment variables.
 */
export const ApiKeySelector: React.FC = () => {
  return null;
};
